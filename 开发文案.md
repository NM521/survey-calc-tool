# 测绘计算器程序开发报告

## 项目概述

| 项目信息 | 内容 |
|---------|------|
| 项目名称 | 测量计算助手 v1.0 |
| 开发人员 | 吴尚 |
| 学号 | 202406010105 |
| 专业 | 测绘工程 |
| 开发语言 | Python 3.7+ |
| 开发日期 | 2026 年 6 月 |

---

## 一、选题背景

测绘工程是研究地理空间信息的采集、处理、分析和表达的一门学科。在《数字测图原理与方法》《控制测量学》《误差理论与测量平差》等核心课程中，全站仪导线测量、水准测量和坐标计算是最基础、最频繁的工作内容。传统手工计算效率低、易出错，开发一款自动化计算工具具有实际意义。

本程序基于 Python 编程语言，将课堂所学的测量学理论转化为可运行的软件工具，具备全站仪导线计算、水准测量计算、单点坐标计算三大功能模块。

## 二、功能模块

### 2.1 全站仪导线测量
- 功能：支持附合导线坐标计算，采用右角法进行方位角传递
- 输入：起始点数据（点名、X/Y/H 坐标、起始方位角）、各测站观测角和水平距离
- 输出：各导线点的三维坐标、方位角、象限角、边长

**核心公式：**
- 方位角传递：α前 = α后 + 180° − β右
- 坐标增量：ΔX = S · sin(α)，ΔY = S · cos(α)
- 象限角：将 [0°, 360°) 方位角转换为 N/S/E/W 格式

### 2.2 水准测量
- 功能：多测站水准测量数据自动计算，闭合差判定
- 输入：已知点 BM1/BM2 高程、各测站后视/前视读数及测站距
- 输出：高差、高程、累计距、闭合差及合格性（绿色=合格，红色=不合格）

**核心公式：**
- 高差：Δh = b − f（后视 − 前视）
- 高程传递：H前 = H后 + Δh
- 闭合差：fh = Σh测 − (H终 − H始)
- 允许闭合差：fh允 = 12√n mm（n 为测站数）

### 2.3 单点坐标计算
- 功能：根据测站点和观测数据批量计算目标点三维坐标
- 输入：测站点坐标、方位角、仪器高/棱镜高、各目标点水平角/竖直角/斜距
- 输出：目标点 X/Y/H 坐标、水平距、高差、方位角、象限角

**核心公式：**
- 水平距离：D = S · sin(Z)（Z 为天顶距）
- 高差：Δh = S · cos(Z) + i − v
- 目标方位角：α目标 = α站 + β

### 2.4 CSV 数据导出
所有计算结果均支持一键导出为 CSV 文件，UTF-8 编码（含 BOM 头），可在 Excel 中直接打开。

## 三、技术架构

### 3.1 整体架构

```
┌─────────────────────────────────────┐
│            用户界面层               │
│  Bootstrap 5 + HTML5 + JavaScript  │
│      (响应式布局，适配手机/电脑)     │
└─────────────┬───────────────────────┘
              │  HTTP / JSON
┌─────────────▼───────────────────────┐
│           应用服务层                │
│         Flask (Python Web框架)      │
│      RESTful API 接口              │
└─────────────┬───────────────────────┘
              │  函数调用
┌─────────────▼───────────────────────┐
│           计算引擎层                │
│      Python math 标准库            │
│  角度计算 / 坐标变换 / 平差计算     │
└─────────────────────────────────────┘
```

### 3.2 技术栈明细

| 层级 | 技术 | 说明 |
|------|------|------|
| 前端 UI | Bootstrap 5 + Font Awesome | 响应式、现代风格 |
| 前端交互 | JavaScript (Fetch API) | AJAX 异步数据请求 |
| 后端框架 | Flask | Python Web 微框架 |
| WSGI 服务器 | gunicorn | 生产环境部署 |
| 桌面封装 | pywebview | 内嵌 Chromium 浏览器 |
| 公网隧道 | serveo.net (SSH -R) | 免费内网穿透 |
| 云部署 | Render.com | PaaS 平台免费层 |
| 计算引擎 | Python math | 三角函数、坐标运算 |
| 数据导出 | CSV (Python csv) | UTF-8 编码输出 |

### 3.3 项目文件结构

```
测量计算助手/
├── main.py              # 桌面窗口主入口（pywebview）
├── run_server.py         # Flask HTTP 服务器
├── tunnel.py             # 公网隧道一键启动脚本
├── api.py                # 核心计算引擎
├── requirements.txt      # Python 依赖清单
├── templates/
│   └── index.html        # 前端主界面（Bootstrap 美化）
├── static/
│   ├── css/style.css     # 自定义样式表
│   └── js/main.js        # 前端交互逻辑
├── .gitignore            # Git 忽略文件配置
├── README.md             # 使用说明
└── 开发文案.md            # 本文档
```

## 四、运行方式

本程序支持三种运行模式：

### 4.1 桌面窗口模式
```bash
python main.py
```
弹出独立窗口，内嵌 Chromium 浏览器渲染界面，体验类似原生桌面应用。

**依赖：** `pip install flask pywebview`

### 4.2 浏览器模式（局域网）
```bash
python run_server.py
```
浏览器访问 `http://127.0.0.1:5555`，手机连接同一 WiFi 可通过本机 IP 访问。

**依赖：** `pip install flask`

### 4.3 公网隧道模式（远程分享）
```bash
python tunnel.py
```
一键启动后输出公网临时网址（形如 `https://xxx.serveousercontent.com`），任何人通过浏览器即可访问，无需安装任何软件或连接同一网络。

**原理：** 通过 SSH 反向隧道技术（serveo.net），将本地 Flask 服务的 5555 端口映射到公网 80 端口。

### 4.4 云平台部署模式（永久在线）

**部署平台：** Render.com（PaaS 云平台，免费层）

**部署步骤：**
1. 创建 GitHub 仓库，推送代码至 `https://github.com/NM521/survey-calc-tool`
2. 在 Render.com 注册并关联 GitHub 账号
3. 创建 Web Service，指向上述仓库
4. 配置参数：
   - Build Command: `pip install -r requirements.txt`
   - Start Command: `gunicorn run_server:app`
   - Instance Type: Free
5. 部署完成后获得永久在线网址（形如 `https://survey-calc-tool.onrender.com`）

**优势：** 24 小时永久在线，无需本地开机，任何人任何设备均可访问。

## 五、关键技术实现

### 5.1 角度计算
测绘中角度范围为 [0°, 360°)，超过范围需归一化。程序实现了 `angle_normalize()` 函数，使用取模运算完成角度标准化。所有三角函数计算均采用 Python math 库的 `radians()` 和 `degrees()` 进行单位转换，确保计算精度。

### 5.2 象限角转换
方位角（0°–360°）需根据所在象限转换为 N–S–E–W 格式的象限角：
- 第 I 象限 (0°–90°)：Nα°E
- 第 II 象限 (90°–180°)：S(180−α)°E
- 第 III 象限 (180°–270°)：S(α−180)°W
- 第 IV 象限 (270°–360°)：N(360−α)°W

### 5.3 前后端数据交互
前端 JavaScript 使用 Fetch API 以 JSON 格式 POST 数据到 Flask API，后端计算后返回 JSON 结果，前端动态渲染 HTML 表格。CSV 导出在前端使用 Blob + Data URL 技术本地完成，无需服务器参与。

### 5.4 响应式布局
使用 Bootstrap 5 的 Grid 系统（col-lg-5 / col-md-7 等断点类），自动适配电脑（1200px+）、平板（768px–1199px）、手机（<768px）三种屏幕尺寸。

### 5.5 SSH 反向隧道
利用 serveo.net 免费服务，通过 `ssh -R 80:127.0.0.1:5555 serveo.net` 命令建立从公网到本地的 TCP 反向隧道，外部请求通过 serveo 代理转发至本地 Flask 服务。

### 5.6 云平台部署
采用 Render.com 的 PaaS 服务，通过 gunicorn WSGI 服务器运行 Flask 应用，实现生产环境的自动化部署和持续集成（Git Push → Auto Deploy）。

## 六、测试验证

### 6.1 导线计算测试
- 输入：起始点 A(1000, 1000, 50)，方位角 45°，两个测站观测角均为 90°，平距 100m
- 输出：测点1(1070.711, 929.289)、测点2(1000.000, 858.579)，验证通过

### 6.2 水准计算测试
- 输入：BM1=100.000m，后视 1.500m，前视 1.000m，测站距 50m
- 输出：高差 +0.500m，TP1 高程 100.500m，闭合差计算正常，验证通过

### 6.3 单点坐标测试
- 输入：测站点(1000, 1000, 50)，方位角 45°，水平角 30°，天顶距 85°，斜距 100m
- 输出：目标点(1096.225, 1025.783, 58.716)，验证通过

### 6.4 公网访问测试
- 启动 tunnel.py，获得公网 URL
- 使用 Python urllib 请求该 URL，返回 200 OK，内容长度 20679 字节，验证通过

## 七、总结与展望

### 7.1 项目总结
本程序作为测绘工程专业 Python 课程的结课设计，将《数字测图原理与方法》《控制测量学》《误差理论与测量平差》等课程的理论知识与 Python 编程实践相结合，实现了全站仪导线测量、水准测量、单点坐标计算三大核心功能模块。

技术层面，采用 Flask + Bootstrap 5 的前后端分离架构，同时支持桌面窗口（pywebview）、局域网浏览器、公网隧道（serveo.net）、云平台部署（Render.com）四种运行模式。项目代码已开源至 GitHub，可通过 Render 云平台提供永久在线的公共服务。

### 7.2 未来展望
- 增加更多平差方法（条件平差、间接平差、秩亏自由网平差）
- 集成 GNSS 数据处理（基线解算、网平差）
- 支持 DXF 格式测量图形输出
- 增加 SQLite 数据库，实现测量数据持久化存储
- 增加用户登录和项目管理功能

## 参考文献

[1] GeoscienceAustralia/GeodePy — A toolkit for Geodesy and Surveying in Python. https://github.com/GeoscienceAustralia/GeodePy  
[2] mikemost/geomatics — Python utilities for geomatics engineering numerical methods. https://github.com/mikemost/geomatics  
[3] geospace-code/pymap3d — Pure-Python 3D coordinate conversions for geospace. https://github.com/geospace-code/pymap3d  
[4] 武汉大学测绘学院. 数字测图原理与方法. 武汉大学出版社  
[5] 武汉大学测绘学院. 误差理论与测量平差. 武汉大学出版社  
[6] Flask 官方文档. https://flask.palletsprojects.com/  
[7] Render 官方文档. https://render.com/docs  
[8] Bootstrap 5 官方文档. https://getbootstrap.com/