# 测绘计算器程序开发报告

## 项目概述

**项目名称：** 测量计算助手 v1.0  
**开发人员：** 吴尚  
**学号：** 202406010105  
**专业：** 测绘工程  
**开发语言：** Python 3.7+  
**开发日期：** 2026 年 6 月  

---

## 一、项目背景与意义

测绘工程是研究地理空间信息的采集、处理、分析和表达的一门学科。在实际测量工作中，全站仪导线测量、水准测量和坐标计算是最基础、最核心的工作内容。传统的手工计算方式不仅效率低下，而且容易出错。

本程序选题立足于测绘工程本科课程教学大纲，将《数字测图原理与方法》《误差理论与测量平差》《控制测量学》等课程中的核心计算内容用 Python 编程实现，帮助测量人员进行快速、准确的数据处理和结果输出。

## 二、功能模块

### 2.1 全站仪导线测量模块

实现了附合导线坐标计算，支持右角法计算方位角和坐标。用户输入起始点数据（点名、X、Y、H 坐标、起始方位角）和各测站的观测角与水平距离后，系统自动计算出各导线点的三维坐标、方位角和象限角。

**核心公式：**
- 右角法方位角传递：α前 = α后 + 180° - β右
- 坐标增量计算：ΔX = S·sin(α)，ΔY = S·cos(α)

### 2.2 水准测量模块

实现了多测站水准测量数据的自动计算，包括高差、高程的推算，以及闭合差的计算和合格性判断。系统自动将闭合差与允许值比较，绿色显示表示合格，红色显示表示不合格。

**核心公式：**
- 高差：Δh = b - f（后视读数 - 前视读数）
- 高程：H前 = H后 + Δh
- 闭合差：fh = Σh测 - (H终 - H始)
- 允许闭合差：fh允 = 12√n mm（n 为测站数）

### 2.3 单点坐标计算模块

根据已知测站点的坐标和观测数据（水平角、竖直角、斜距），批量计算多个目标点的三维坐标。

**核心公式：**
- 水平距离：D = S·sin(Z)（Z 为天顶距）
- 高差：Δh = S·cos(Z) + i - v
- 目标方位角：α目标 = α站 + β

## 三、技术架构

采用前后端分离架构，前端使用 Bootstrap 5 构建现代化响应式用户界面，后端使用 Flask 提供 RESTful API，计算引擎基于 Python 标准库实现。

| 层级 | 技术 |
|------|------|
| 前端 | HTML5 + CSS3 + JavaScript + Bootstrap 5 |
| 后端 | Python 3 + Flask |
| 桌面封装 | pywebview（内嵌 Chromium） |
| 计算引擎 | Python math 标准库 |
| 公网隧道 | serveo.net（SSH 反向隧道） |

## 四、运行方式

### 方式一：桌面窗口版
```bash
python main.py
```
启动后弹出独立桌面窗口，体验类似原生桌面应用。

### 方式二：浏览器版（局域网）
```bash
python run_server.py
```
浏览器访问 http://127.0.0.1:5555，手机连同一 WiFi 可访问。

### 方式三：公网隧道版（推荐分享使用）
```bash
python tunnel.py
```
一键启动后输出公网网址（形如 https://xxx.serveousercontent.com），任何人用手机或电脑浏览器打开即可使用，不需要安装任何软件，不需要连接同一个 WiFi。

## 五、项目文件结构

```
测量计算助手/
├── main.py                  # 桌面窗口主入口
├── run_server.py            # 浏览器模式服务器
├── tunnel.py                # 公网隧道一键启动
├── api.py                   # 核心计算逻辑
├── templates/
│   └── index.html           # 前端界面（Bootstrap 美化）
├── static/
│   ├── css/style.css        # 自定义样式
│   └── js/main.js           # 前端交互逻辑
├── README.md                # 使用说明
└── 开发文案.md               # 本文档
```

## 六、技术难点与解决方案

### 6.1 角度规范化计算
测绘角度范围 [0°, 360°)，所有三角函数均使用十进制度，实现了完整的角度归一化和差值计算函数。

### 6.2 方位角象限判断
方位角需根据象限转换为 N-S-E-W 格式的象限角表示，实现了自动转换。

### 6.3 前后端数据交互
前端使用 Fetch API 将 JSON 格式数据 POST 到 Flask 后端，后端计算后返回 JSON 结果，前端动态渲染表格。CSV 导出在前端本地完成。

### 6.4 公网隧道穿透
通过 SSH 反向隧道技术（serveo.net），无需云服务器即可将本地程序暴露到公网，生成临时网址供他人直接访问。

### 6.5 响应式布局
使用 Bootstrap 5 Grid 系统，自动适配电脑、平板、手机等不同屏幕尺寸。

## 七、总结与展望

本程序将测量学理论与 Python 编程实践相结合，实现了全站仪导线、水准测量、单点坐标计算三大核心功能。采用 Flask + pywebview 架构，同时支持桌面窗口、局域网浏览器、公网隧道三种运行模式。

未来可扩展方向：
- 增加更多测量平差方法（条件平差、间接平差）
- 支持 DXF 图形输出
- 集成 GNSS 数据处理功能

## 参考文献

1. GeoscienceAustralia/GeodePy — https://github.com/GeoscienceAustralia/GeodePy
2. mikemost/geomatics — https://github.com/mikemost/geomatics
3. geospace-code/pymap3d — https://github.com/geospace-code/pymap3d
4. 数字测图原理与方法，武汉大学出版社
5. 误差理论与测量平差，武汉大学出版社